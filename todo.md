# InvestDesk TODO

## 基盤
- [x] DB スキーマ設計（holdings / watchlist / news / signals / investmentCard / snapshots / importJobs / settings）
- [x] マイグレーション適用
- [x] デザイントークン（index.css）とフォント設定
- [x] DashboardLayout ベースのナビゲーション構築
- [x] 市場データ取得サービス（株価・企業プロファイル）
- [x] tRPC ルーター（portfolio / news / watchlist / import）

## 1. スクリーンショット OCR 取込
- [x] 画像アップロード（S3 storagePut）
- [x] LLM Vision による銘柄コード・株数・取得単価・現在値の抽出
- [x] 抽出結果の確認・編集・承認 UI
- [x] 承認後の DB 保存（新規追加／既存更新のマージ）

## 2. 総資産ダッシュボード
- [x] 総時価評価額・総損益（金額・%）
- [x] 業種別分布 円グラフ
- [x] 通貨別分布
- [x] ポジション集中度アラート
- [x] 資産推移グラフ

## 3. 保有銘柄一覧
- [x] 銘柄コード・名称・株数・取得単価・現在値・損益の一覧表示
- [x] AI シグナル（ADD/HOLD/WATCH/REDUCE/EXIT）バッジ表示
- [x] ソート・フィルタ
- [x] 手動追加・編集・削除

## 4. 企業投資カード
- [x] 買付理由・コア投資ロジック・バリュエーション前提・主要決算数値・エグジット条件（API）
- [x] 手動編集フォーム
- [x] 銘柄別詳細ページ（価格チャート・ニュース・シグナル履歴）

## 5. ニュースモニタリング & AI 分析
- [x] ニュース取得 API 連携（Google News RSS）
- [x] AI によるポジティブ／ネガティブ判定と影響度スコア
- [x] 原文リンク付与
- [x] ニュース一覧ページ（銘柄・評価・影響度でのフィルタ、キーワード検索）

## 6. AI 意思決定シグナルエンジン
- [x] ニュースセンチメント・価格変動・バリュエーション乖離の統合
- [x] ADD/HOLD/WATCH/REDUCE/EXIT の判定
- [x] 根拠テキストの生成と保存
- [x] シグナル再生成の手動トリガー

## 7. ウォッチリスト
- [x] 目標価格・買付条件・注目理由の記録（API）
- [x] 追加・編集・削除（API）
- [x] 保有ポジションへの昇格
- [x] 目標価格到達アラート表示
- [x] ウォッチリストページ UI

## 8. 株価自動更新
- [x] 日本株・米国株の価格取得
- [x] 保有銘柄・ウォッチリスト全銘柄の一括更新
- [x] 手動更新ボタン
- [x] 定期ジョブのコールバック実装（/api/scheduled/syncPrices, /api/scheduled/syncNews）
- [x] Heartbeat スケジュール登録（株価: 平日 JST 15:30 と翌 6:30、ニュース: 毎日 JST 7:00。詳細は docs/scheduled-jobs.md）

## ページ実装
- [x] ダッシュボード
- [x] 保有銘柄一覧
- [x] 銘柄詳細ページ（投資カード編集含む）
- [x] ウォッチリストページ
- [x] ニュース一覧ページ
- [x] スクリーンショット取込ページ
- [x] 設定ページ
- [x] App.tsx ルーティング登録

## 品質
- [x] vitest テスト作成（51 件パス：シンボル解決 19 / パスコード 16 / ニュースクエリ 10 / OCR 正規化 5 / 認証 1）
- [x] 免責事項の明示（投資助言ではない旨）
- [x] スクリーンショット検証（全ページ描画確認）
- [x] tRPC Provider ネスト順の不具合修正（データ取得が動作しない問題）
- [x] 銘柄詳細ページの構成確認（投資カード / 価格チャート / ニュース / シグナル履歴の 4 タブ、保存処理と loading・empty 状態を実装済み）
- [x] レスポンシブ確認（390px 幅、ページ別）
  - [x] ダッシュボード：サイドバーがドロワー化しヘッダーにトグル、操作ボタンが折返し表示
  - [x] 保有銘柄：検索・フィルタ・並び替えが縦積み、空状態が中央表示
  - [x] ウォッチリスト：見出しと操作ボタンが縦積み
  - [x] ニュース：キーワード検索とフィルタ 3 種が折返し表示
  - [x] 設定：入力欄が 1 カラム化、保存ボタンが右下に固定表示
  - [x] スクショ取込：ドロップゾーンとヒントが縦積み
- [x] 実データでの OCR 精度確認（楽天証券 8 銘柄すべて正しく抽出、整合性チェック 16 項目が誤差 0.00%。詳細は docs/ocr-verification.md）
- [x] 逆算した取得単価の丸め処理を追加（3389.315789... → 3389.32）

## 9. 簡易パスコード認証（Manus ログインの置き換え）
- [x] パスコードのハッシュ保存用テーブル追加（passcodeAuth）
- [x] パスコード検証 tRPC プロシージャ（公開）とトークン発行
- [x] パスコード変更プロシージャ
- [x] 全データアクセスをパスコードセッションで保護（Bearer トークンを context で解決）
- [x] ロック画面 UI（数字キーパッド、物理キーボード対応、エラー表示とシェイク）
- [x] 設定ページにパスコード変更フォームを追加（初期値警告つき）
- [x] Manus ログイン画面を経由しないことを確認（useAuth / startLogin の参照を除去）
- [x] 総当たり対策（連続 5 回失敗で 15 分ロック）
- [x] scrypt によるハッシュ化とタイミング攻撃対策
- [x] パスコード認証のテスト作成（16 件）
- [x] トークンを localStorage に保存し次回以降の入力を省略
- [x] 解錠時に OK ボタンが回り続ける不具合を修正（utils.invalidate() の完了待ちを外し、解錠直後は auth.me の検証を待たずに通す。30 秒のタイムアウトも追加）

## 11. スクショフォーマットの学習（moomoo 日本版）
- [x] OCR プロンプトの誤ったラベルを修正（moomoo 日本版を iSPEED と誤記していた）
- [x] プラットフォーム別のレイアウト定義を保持する仕組み（server/services/brokerFormats.ts）
- [x] moomoo 日本版の列構成・既知の癖をレイアウト定義として登録
- [x] 取込時に証券アプリを選択できる UI（選択は次回以降も保持）
- [x] 画面から証券アプリを自動判定し、選択と食い違う場合に通知
- [x] レイアウト定義を渡した状態で実データ検証（8 銘柄・整合性 16 項目すべて誤差 0.00%）
- [x] フォーマット定義のテスト（10 件）

## 12. 本番でのアップロード失敗の修正
- [x] 原因特定：INSERT の戻り値から ID を取り出せず NaN になり、履歴更新の WHERE 句が壊れていた
- [x] insertId の取得を戻り値の形（単体 / 配列 / bigint / 文字列）に依存しない実装に統一
- [x] ID が取得できない場合は NaN を後段に流さず即座に失敗させる
- [x] 履歴用ジョブの作成・更新に失敗しても読み取り自体は継続できるようにする
- [x] insertId 抽出のテスト（8 件）
- [x] 開発環境で取込が成功することを確認（8 銘柄、jobId 採番も正常）

## 10. GitHub リポジトリへのコード反映
- [x] kokumin1010-commits/investdash へプッシュ（164 ファイル）
- [x] 機密ファイルが含まれないことを確認（環境変数ファイルは .gitignore 済み、env.ts は参照のみ）
- [x] README にセットアップ手順・機能一覧・移行時の注意を記載

## 13. 証券口座マスタ（brokerAccounts）による本格的な口座管理【未実装：必要になってから着手】
セクション 21（プラットフォーム単位の `holdings.broker`）とセクション 28（同一銘柄の
複数口座保有）で、実用上必要な範囲はすでに満たしている。
以下が必要になるのは「**同一プラットフォーム内に 2 つ以上の口座**を持つ」ケースのみ
（例: moomoo で本人名義と特定口座を分けたい、楽天で 2 口座持つ）。
現状は moomoo 1 口座 + 楽天 1 口座なので着手しない。要望が出た時点で実装する。
- [~] brokerAccounts テーブル追加（口座名・プラットフォーム・国・基準通貨）→ 保留（同一プラットフォーム内の複数口座を持っていないため）
- [~] holdings / watchlist を brokerAccountId 参照へ移行 → 保留（同上）
- [~] 口座の追加・編集・削除 API と UI → 保留（同上）
- [~] スクショ取込で「証券アプリ」ではなく「保存先口座」を選択 → 保留（現状は証券アプリ選択で一意に決まる）
- [~] ダッシュボードに国別の資産内訳を追加 → 保留（現在の保有は全て日本株のため通貨別分布で足りている。米国株を持った時点で着手）

## 14. 他プラットフォームのフォーマット学習【保留：実画面の提供待ち】
レイアウト定義は実際の画面を見ないと精度を担保できないため、スクリーンショットを
受け取ってから追加する。定義の追加先は `server/services/brokerFormats.ts`。
- [x] 楽天証券 iSPEED のレイアウト定義（下記セクション 27 で完了）
- [~] 富途牛牛 / Futu のレイアウト定義 → 保留（実画面の提供待ち。画面をもらえば即座に定義できる）

## 15. iOS Safari でのアップロードエラーとモバイル表示の修正
- [x] 原因特定：iOS Safari の FileReader.readAsDataURL が TypeError を投げるケースがある
- [x] 画像読み込みを Blob URL + canvas 経由の JPEG 変換に変更（FileReader はフォールバック）
- [x] HEIC / HEIF を受け付ける（canvas 経由でブラウザがデコードできれば JPEG に変換）
- [x] file.type が空でも拡張子で画像判定（iOS の写真アプリ経由対策）
- [x] 長辺 2200px を超える画像は自動縮小してアップロード失敗を防ぐ
- [x] 変換中はボタンに進行表示を出す
- [x] 失敗時にファイル名と理由を個別に表示する
- [x] 証券アプリ選択カードのモバイルレイアウト崩れを修正（縦積みに変更）
- [x] 画像変換ユーティリティのテスト（4 件）
- [x] クライアント側テストを vitest の対象に追加（計 73 件パス）

## 16. モバイルでの見やすさ改善
- [x] 取込確認画面をスマホではカード表示に切り替える（横スクロールの表をやめる）
- [x] 株数・取得単価の入力欄を高さ 44px に拡大して数字が読めるようにする
- [x] 保有一覧ページもスマホでカード表示にする（評価額・損益を大きく、明細を 4 列で表示）
- [x] ニュース一覧・ウォッチリストのモバイル表示を確認（縦積みで問題なし）
- [x] 実機相当の幅（390px / 430px）で全ページを確認
- [x] 開発時のみ ?devToken= でロック画面を通過できるようにし、モバイル表示の検証を可能にした

## 18. 株価取得の不具合修正
- [x] Data API のクエリに真偽値を渡していて 400 エラーになっていた問題を修正
- [x] 全 18 銘柄の株価更新が成功することを確認

## 17. 追加スクショ（2 枚目）の銘柄登録とデータ照合
- [x] 2 枚目のスクショに写っていた 11 銘柄をアプリ経由で登録する（新規 10 / 更新 1）
- [x] 登録結果を元画面と 1 件ずつ照合する検証スクリプトを作成する（scripts/verify-holdings.mjs）
- [x] 切れて登録された銘柄名を正式名称に補完する（オリエンタルランド / 富士フイルムホールディングス / ANAホールディングス / さくらインターネット）
- [x] 照合結果：18 銘柄すべて一致（株数・取得単価・現在値・評価額・損益）
- [x] 業種はすべて取得済み

## 19. 3 枚目のスクショの銘柄登録
- [x] 3 枚目（IMG_7598）の 9 銘柄を登録する
- [x] 全 27 銘柄を元画面と照合する（株数・取得単価・銘柄名すべて一致）
- [x] 純資産との整合を確認する（評価額 88,830,390 + 預り金 1,255,302 = 90,085,692 ≒ 元画面 89,759,882）
- [x] 省略表示されていた銘柄名を正式名称で登録（SBI / みずほ / パナソニック / 野村 ホールディングス）

## 20. AI 利用枠の上限に達したときの案内
- [x] 生のエラー文（412 Precondition Failed）ではなく、原因と対処が分かる文言に変換する
- [x] 手入力での代替手段を案内に含める

## 21. どの銘柄がどのプラットフォームかを一目で分かるようにする
ユーザー要望: 「日本moomooで書いたほうがよいね どの銘柄がどのプラットフォームか」
「ぱっとみわかるように」
- [x] holdings に証券プラットフォーム（broker）を保持するカラムを追加
- [x] 既存 27 銘柄を moomoo 日本版として移行
- [x] プラットフォームごとの色定義（moomoo=オレンジ / 楽天 iSPEED=赤 / 富途=青 / その他=グレー）
- [x] 保有一覧のカードに色付きバッジを表示（スマホ）
- [x] 保有一覧のテーブルにも「口座」列を追加（デスクトップ）
- [x] 銘柄詳細ページにバッジを表示
- [x] スクショ取込時に選択した証券アプリを銘柄に紐づけて保存
- [x] ダッシュボードに証券口座別の資産内訳（評価額・損益・構成比）を表示
- [x] 保有一覧でプラットフォームによる絞り込みを追加（2 口座以上ある場合のみ表示）
- [x] 銘柄追加・編集フォームでプラットフォームを選べるようにする
- [x] ブローカー表示ユーティリティのテストを追加（79 件パス）

## 22. ダッシュボード上部に口座別の資産額を表示
ユーザー要望: 「Top ページに moomoo いくらあるか、ほかいくらあるか、分かるとよいね」
現状の「証券口座別」カードは画面下部にあり、スマホだとスクロールしないと見えない。
- [x] 口座別の内訳をダッシュボード上部（サマリーカード直下）へ移動
- [x] 口座ごとにカード化し、評価額を大きく・含み損益・構成比・銘柄数を併記
- [x] 下部にあった重複カードを削除
- [x] スマホ幅で表示を確認（サマリー直下に表示されることを確認）

## 23. シグナル機能が使えない・意味が分からない
ユーザー指摘: 「シグナルとは？押しても意味ない」「未生成はどういうこと？」
アプリの中核機能なのに、押しても反応がなく、用語の説明もないため何をするものか伝わっていない。
- [x] 原因特定: Manus アカウントの AI 利用枠が上限（412 usage exhausted）。生のエラーが画面に出ず無反応に見えていた
- [x] `server/services/aiErrors.ts` を新設し、利用枠上限・認証エラー・タイムアウトを対処法付きの日本語に変換
- [x] シグナル生成・取込 OCR の両方でエラー変換を適用（実 API で TOO_MANY_REQUESTS と日本語文言を確認）
- [x] 全銘柄分析は利用枠切れを検知したら残りを打ち切り、成功件数を明示（27 銘柄分の無駄な待ちを回避）
- [x] ボタンのラベルを「シグナル」→「AI分析」に変更、実行中は「分析中…」＋スピナー表示
- [x] 生成成功時は判定理由の冒頭もトーストに表示
- [x] `SignalGuide` を作成し、ADD/HOLD/WATCH/REDUCE/EXIT の意味と判断材料を保有一覧に常設
- [x] 「未生成」バッジにツールチップを追加し、どうすれば生成されるか明示
- [x] ニュース 0 件でも価格データのみで分析可能なことを確認（`analysis.ts` の newsLines が 0 件時も成立）
- [x] AI エラー変換のテストを追加（86 件パス）
- [x] 利用枠回復後に実データで検証 → 別の不具合を発見（下記 24 で対応）

## 24. AI が JSON ではなく Markdown を返しシグナル生成が失敗する
利用枠が回復して初めて表面化した不具合。`## 判定結果` という Markdown が返り JSON 解析に失敗していた。
- [x] 原因特定: 内蔵プロキシでは `response_format: json_schema` を指定してもモデルにより無視される
  - claude-sonnet-4-6 / claude-haiku-4-5 → Markdown を返す（NG）
  - gpt-5-mini → JSON を返すが `max_tokens` では出力が途中で切れる（`max_completion_tokens` 必須）／35 秒で遅い
  - gemini-3-flash-preview → JSON を安定して返し 10〜16 秒（採用）
- [x] シグナル生成・ウォッチリスト判定・ニュース分析のモデルを `gemini-3-flash-preview` に統一
- [x] `server/services/jsonExtract.ts` を新設し、コードフェンス／前置き文があっても JSON を抽出できる安全網を追加
- [x] OCR のパースにも同じ安全網を適用（同種の失敗を予防）
- [x] JSON 抽出のテストを追加（ネスト括弧・文字列内の括弧・エスケープ引用符を含む 10 件）
- [x] 実データで SUBARU のシグナル生成が成功することを確認（WATCH / 確信度30 / 5観点の評価付き）
- [x] 27 銘柄すべてで一括分析が通ることを確認（約 4 分 30 秒 / 失敗 0 件）

## 25. 全銘柄が WATCH に張り付き判定が役に立たない
旧ルール「投資カード未記入なら WATCH を基本とする」により、ニュースの内容に
関係なく投資カードの有無だけで結論が決まっていた。27 銘柄分のカードを手書きするのは
非現実的なため実用にならない。
- [x] ニュースを 365 件取得し全件 AI 分析（好材料163 / 中立147 / 悪材料55）
- [x] ニュース取得後も全 27 銘柄 WATCH のままであることを確認し、原因をルールと特定
- [x] 判定ルールを修正: 投資カード未記入でも客観データ（ニュース影響度・52週レンジ内の位置・騰落率・構成比）から判定する
- [x] confidence の目安を 3 段階で明示（カード有 70-90 / カード無+材料有 45-65 / 材料乏しい 40 未満）
- [x] 修正後に判定が分岐することを確認（ADD 3 / HOLD 14 / WATCH 10、平均確信度 54-62）
- [x] 個別の妥当性を確認（三井物産→ADD、東京地下鉄→WATCH、SUBARU→WATCH）
- [x] デスクトップ表のシグナルボタンもツールチップを「AI分析でシグナルを生成」／実行中は「分析中…」に統一
- [x] ダッシュボード上部の口座別カードをデスクトップ幅（1280px）でも確認（サマリー直下に表示、崩れなし）
- [x] ニュース 0 件・投資カード未作成・価格未取得の各ケースでプロンプトが成立することをテストで裏付け
- [x] `generateSignal` を LLM モック付きで検証し、ニュース 0 件でも最後まで通ることを確認（94 件パス）

## 26. 本番で一括処理が途中切断される（Autoscale の 180 秒制限）
本番（Autoscale / Cloud Run）のリクエスト上限は 180 秒。一括 AI 分析は約 5 分、
ニュース取得は約 12 分かかるため、画面のボタンから実行すると必ず切断されていた。
開発環境では制限がないため気づきにくい類の不具合。
- [x] `regenerateAllSignals` を `offset` / `batchSize` で分割実行できるようにする（既定 6 件）
- [x] `news.syncAll` / `syncNewsForUser` も同様に分割対応（既定 4 件）
- [x] サーバーが `nextOffset`・`processed`・`total` を返し、完了判定をサーバー側で持つ
- [x] クライアントに `useBatchRun` フックを追加し、`nextOffset` を辿って完走させる
- [x] ボタンに進捗を表示（「AI分析中 12/27」「取得中 8/27 銘柄」）
- [x] AI 利用枠切れを検知したバッチで後続を打ち切る
- [x] 二重起動の防止（実行中は他の一括ボタンも無効化）
- [x] 定期実行（Heartbeat）は従来どおり全件処理（`batchSize` 省略時は全件）
- [x] 分割ロジックのテストを追加（取りこぼしなし・必ず終了・空配列・範囲外 offset / 8 件）
- [x] 実データで検証: 27 銘柄を 5 バッチで完走、各バッチ 39〜76 秒（上限 180 秒の最大 42%）、失敗 0 件
- [x] 分割実行後の判定分布を確認（ADD 4 / HOLD 13 / WATCH 10）
- [x] GitHub（kokumin1010-commits/investdash）へプッシュ（b532d38 / 旧トークン失効のため新 PAT で実施）
- [x] 本番の稼働確認（/ ・/holdings ・/news すべて 200、ランタイムログにエラー 0 件）

## 27. 楽天証券 iSPEED のフォーマット学習
実画面 3 枚（ビュー A 取得単価 / B 評価額 / C 指標）を受領。iSPEED は列見出しの
「‹ ›」で横スクロールでき、見えている列が変わるため moomoo と違い 3 通りの画面が存在する。
- [x] 実画面から列構成・特有の癖を読み取る
- [x] 3 つのビュー（A 取得単価 / B 評価額 / C 指標）それぞれの扱いを layoutPrompt に定義
- [x] ビュー A のみを取り込み対象とする方針を決定（評価額・損益は計算で導ける）
- [x] ビュー B と突き合わせて計算値が一致することを確認（ヤクルト・SUMCO・住友化学・産業ファンドで完全一致）
- [x] ビュー C（PER/PBR）を取り込まない判断（赤字企業・REIT が 0.00 表示され判定を歪めるため。API から取得する）
- [x] 「（執行中）」列を保有数量と誤認しないよう明示
- [x] 省略された銘柄名の補完表を定義（3249/4661/4689/4751/4816/4927）
- [x] 「貸株中」バッジ・REIT の投資口・moomoo と逆の配色（赤=プラス）を定義
- [x] 純資産・預り金が表示されない画面なので cash を null にすると定義
- [x] 実データ検証（ビュー A）: 11 銘柄すべて抽出、整合性 22 項目すべて誤差 0.00%、所要 37.9 秒
- [x] 実データ検証（ビュー B）: 数量・取得単価を推測せず null にすることを確認
- [x] 実データ検証（ビュー C）: 空配列＋案内文を返すことを確認
- [x] レイアウト定義のテストを追加（10 件 / 計 123 件パス）
- [x] 検証記録を docs/ispeed-verification.md に残す
- [x] iSPEED 保有銘柄の実登録（11 銘柄すべて新規登録、更新 0 / スキップ 0）

## 28. 同一銘柄を複数の証券口座で保有している場合の対応
iSPEED 登録時に moomoo と 5 銘柄（ヤクルト・SUMCO・住友化学・オリエンタルランド・資生堂）が
重複していることが判明。従来の一意制約（ユーザー + シンボル）では moomoo のデータが
上書きされて消えるため、合計と口座別内訳の両方を見られるよう設計を変更した。
ユーザー要望: 「かぶってる場合は 合計も見れて 別々も見れるように」
- [x] 重複を検知し、登録前にユーザーへ方針を確認
- [x] DB に userId + symbol + broker の複合インデックスを追加（既存データは無変更）
- [x] DB 層に getHoldingBySymbolAndBroker / listHoldingsBySymbol を追加
- [x] 取込（applyRows）の既存判定を口座込みに変更し、別口座を上書きしないようにした
- [x] 手動追加（addHolding）の重複判定を口座込みに変更
- [x] ウォッチリスト昇格（promote）に broker を追加
- [x] services/groupPositions.ts を新設し、シンボル単位の合計ビューを実装
- [x] 加重平均取得単価で合算（単純平均では誤るため）
- [x] ニュース件数は最大値を採用（合計すると二重計上になる）
- [x] 集中度アラートを合計の構成比で判定するよう変更（口座をまたぐ集中も検知）
- [x] 保有一覧を合計 1 行 + 口座別内訳の展開に変更
- [x] スマホの内訳を 2 段組にして文字の重なりを解消
- [x] 見出しに「33 銘柄（38 口座分）」と両方の数を表示
- [x] 複数口座の銘柄は編集・削除をどの口座か選べるようにする
- [x] ダッシュボードのシグナル内訳・注意銘柄・構成比上位を銘柄単位に変更
- [x] シグナル生成を合計ポジションで行い、口座別の状況も AI に渡すよう変更
- [x] 一括 AI 分析を銘柄単位に重複排除（38 レコード → 33 件。AI 利用枠の二重消費を回避）
- [x] ニュース取得・株価取得も銘柄単位に重複排除
- [x] 合算ロジックのテストを追加（groupPositions 12 件 / dedupeSymbols 7 件）
- [x] 実データ検証: 5 銘柄すべて合計株数・加重平均が内訳から再計算した値と一致
- [x] 実データ検証: SUMCO のシグナル生成が 1 件のみ（重複なし）、一括分析の総件数 33 と一致
- [x] 検証記録を docs/multi-broker-holdings.md に残す
- [x] テスト 142 件パス

## 29. iSPEED 追加スクショ（3 枚）の銘柄登録
IMG_7620 / IMG_7621 / IMG_7623 を受領。いずれもビュー A（取得単価が見える画面）。
2 枚目と 3 枚目は HOYA〜三井住友の範囲が重複しているため、重複除外が必要。
- [x] 3 枚を OCR で読み取る（整合性チェックすべて誤差 0.00%）
- [x] 画像間の重複銘柄を除外する（IMG_7623 の 8001〜8058 は IMG_7621 と重複）
- [x] トヨタ（7203）が 2 行に分かれていた件をユーザーに確認し、加重平均で合算（8,100 株 @2,581.16）
- [x] 登録を実行する（新規 26 / 更新 0 / スキップ 0）
- [x] 元画面と 1 件ずつ照合する（26 銘柄すべて一致・不一致 0 件）
- [x] 株価・業種を取得する（64 件更新・失敗 0 件、業種未取得 0 件）
- [x] 合計値と口座別内訳の整合を確認する（複数口座 13 銘柄すべて一致）
- [x] 楽天分の評価損益が元画面と一致することを確認（+42,151,701 円 vs 画面 +42,151,447 円、差 254 円 = 0.0006%）
- [x] 検証記録を docs/ispeed-additional-holdings.md に残す

## 30. 数字の意味が分かるようにラベルを付ける
ユーザー指摘: ダッシュボード「注意が必要な銘柄」の「+240.67%」「3.0%」が何の数字か分からない。
さらに WATCH 枠の中に緑の大きな含み益が出ていると「良い銘柄なのに注意枠にある」ように見えて矛盾を感じる。
- [x] ラベルのない数字を全画面で洗い出す（保有一覧のスマホカードは既にラベル済みだった。問題はダッシュボードのみ）
- [x] 「注意が必要な銘柄」に含み損益・構成比のラベルを付ける（含み益 / 含み損を自動で出し分け）
- [x] WATCH の理由が分かるようにする（カード説明文に「含み益が大きくても決算悪化などで見直しが必要な場合はここに入る」と明記）
- [x] 構成比上位カードに説明文を追加（「総資産に占める割合が大きい銘柄」）
- [x] pnlLabel を shared/pnlLabel.ts に切り出してテストを追加（5 件）
- [x] スマホ幅でラベルが入っても崩れないことを確認する

## 31. PC 表の口座別内訳を常時表示にする
ユーザー指摘: PC の保有一覧では口座別内訳がツールチップになっており、マウスを乗せないと見えない。
2 口座の合計と内訳をそのまま出してほしい。
- [x] デスクトップ表の内訳をツールチップから常時表示に変更する
- [x] 合計行と内訳行の階層が分かる見せ方にする（├ / └ の記号と背景色で階層を示す）
- [x] 内訳行から編集・削除ができることを維持する（合計行の重複ボタンは削除して混乱を避けた）

## 32. 評価損益カードに口座別の損益を表示する
ユーザー要望: どの口座でいくら損益が出ているかを総資産ダッシュボードの評価損益カードに出したい。
- [x] 評価損益カードに口座別の金額と率を並べる（楽天 +¥42,151,701 +40.87% / moomoo +¥18,865,445 +26.91%）
- [x] 略記（+¥4215.2万）ではなく正確な金額も分かるようにする
- [x] スマホ幅で崩れないことを確認する

## 33. 証券口座別カードをタップして詳細を見られるようにする
ユーザー要望: 「証券口座別の資産」のカードをタップしたら、その口座の詳細（保有銘柄）が見られるようにしたい。
- [x] 保有一覧ページに口座フィルタを追加する（?broker=rakuten_ispeed のように URL で指定可能）
- [x] ダッシュボードの口座別カードをタップで保有一覧へ遷移させる
- [x] フィルタ適用中は口座名と該当銘柄数・評価額・損益を表示する（楽天 37銘柄 ¥145,284,500 +¥42,151,701 +40.87% / moomoo 27銘柄 ¥88,961,920 +¥18,865,445 +26.91%）
- [x] フィルタを解除できるようにする（「絞り込みを解除」ボタンと口座 Select の両方から）
- [x] 複数口座で持つ銘柄はフィルタ時にその口座の分だけ集計する（合計値のままだと絞り込んだ意味がないため）
- [x] スマホでタップしやすい見た目にする（右端に矢印を表示、hover と押し込みの反応を追加）
- [x] parseBrokerFilter を shared/brokerFilter.ts に切り出してテストを追加（6 件）
- [x] ヘッダの銘柄数を「27 銘柄を表示中（全 51 銘柄）」に切り替えて誤解を防ぐ
- [x] 評価損益カードの金額が折り返す問題を修正

## 34. iSPEED の米国株に対応する
ユーザー提供: iSPEED の米国株「保有銘柄」画面（外貨表示）2 枚。VOO までで全件。
- [x] 米国株用のレイアウト定義（パターン D）を追加。列見出し「ティッカー」「平均取得価額(ドル)」で日本株画面と自動判別する
- [x] OCR 精度を検証（1 枚目 11 銘柄・整合性 22 項目、2 枚目 12 行・整合性 24 項目、すべて誤差 0.00%）
- [x] currency が自動で USD、market が US と判定されることを確認
- [x] 省略された銘柄名 9 件をティッカーから補完（アドバンス…→AMD、バンガー…→VOO など）
- [x] PYPL の 2 建玉を加重平均で合算（356 株 @70.8186）
- [x] 19 銘柄を登録（新規 19 / 失敗 0）
- [x] 元画面の合計損益と照合（画面 +74,746.57 ドル vs 計算 +74,746.61 ドル、差 0.0001%、不一致 0 件）
- [x] 米国株の株価取得を確認（83 レコード更新・失敗 0 件）
- [x] 通貨別分布に USD が出ることを確認（JPY 64 銘柄 89.0% / USD 19 銘柄 11.0%）

## 35. 為替レートを株価更新と同時に自動取得する
現状: 設定値 150 円/ドル固定。iSPEED 画面からの逆算では 179.05 円/ドルで、約 563 万円の差が出る。
- [x] データソースを確認（Yahoo Finance の USDJPY=X。fetchUsdJpyRate は実装済みだが未使用のままだった）
- [x] syncFxRate を実装。取得失敗時・異常値（50 未満 / 500 超 / NaN / Infinity）は既存値を維持する
- [x] 株価更新（syncPrices）の中で為替レートも更新するようにした
- [x] fxAutoUpdate / fxRateUpdatedAt カラムを追加し、取得時刻を保存・表示
- [x] 設定画面に自動取得トグルと「今すぐ取得」ボタンを追加。自動取得中は手動値を送らない
- [x] 定期実行の結果 JSON に fxRate を含めて、cron でも更新されたことを確認できるようにした
- [x] 実測: 150.0000 → 159.3050 に更新。米国株の円換算が ¥29,076,381 → ¥30,880,086 に修正された
- [x] テスト 7 件追加（妥当性チェックの境界値と評価額への影響）
- [x] iSPEED 画面の 179.05 円/ドルは為替手数料・取得時レートを含む円建て損益からの逆算値であり、市場実勢（159.31）を採用するのが正しいと判断

## 36. 国・市場別に資産と損益を分けて表示する
現状: 日本株 64 銘柄と米国株 19 銘柄が混在し、国ごとの成績や配分が見えない。
- [x] 市場別（JP / US / 他）の集計ロジックを追加する（server/services/marketSlices.ts）
- [x] 米国株は現地通貨ベースの損益と円換算後の損益を分けて出す
- [x] ダッシュボードに「国・市場別の資産」カードを追加する（Globe アイコン、緑/藍で口座色と区別）
- [x] カードをタップするとその市場の銘柄だけに絞り込めるようにする（/holdings?market=US）
- [x] 保有一覧に市場フィルタを追加する（口座フィルタと併用可、両方指定時は両条件で集計）
- [x] 将来 台湾株・香港株などを追加しても自動的に分かれることを確認する（Market 型に追加すれば反映）
- [x] テストを追加する（marketSlices.test.ts 11 件）

### 検証結果（実データ）
| 市場 | 銘柄数 | 評価額 | 構成比 | 円換算損益 | 現地通貨損益 |
|---|---|---|---|---|---|
| 日本株 | 51 | ¥234,246,420 | 88.4% | +35.22% | 同じ（円建て） |
| 米国株 | 19 | ¥30,880,086 | 11.6% | +62.76% | +74,747 USD（+62.76%） |

米国株の現地通貨損益 +74,747 USD は iSPEED 画面の +74,746.57 ドルと一致。

### 為替影響の扱いについて（重要）
当初「為替影響額」を出す設計にしたが、正確に求めるには各銘柄を買った時点の
為替レートが必要で、証券アプリのスクショには含まれない。現在レートで逆算すると
必ず 0 になり誤解を招くため、影響額は出さず現地通貨ベースの損益を併記する形にした。

## 37. 「前日比」を「前回記録からの変化」に置き換える
背景: 長期保有が前提で、スクショの取込は月 1 回程度。前日比は判断材料にならず、日々の変動に気を取られる。
- [x] スナップショットから前回記録との差分を計算する（server/services/periodChange.ts）
- [x] 買い増しによる増加と株価変動による増加を分離する
- [x] ダッシュボードの「前日比」カードを「前回記録からの変化」に置き換える
- [x] 経過日数と比較対象の日付を表示する（例: 8/14 → 8/15（1日間））
- [x] 資産推移グラフを月次／日次で切り替えられるようにする（既定は月次）
- [x] 前日比は保有一覧の各銘柄で確認できる状態を維持する（列・並び替えとも残置）
- [x] テストを追加する（periodChange.test.ts 10 件）

### 重要な設計判断: 銘柄追加時は株価変動分を出さない
当初「株価変動分 = 評価額の増減 − 取得原価の増減」で分離する設計にしたが、
実データで検証したところ誤った数字が出ることを確認した。

8/14 → 8/15 の実測値: 評価額 +1.74 億円、うち取得原価 +1.21 億円、差分 +5,336 万円（+59.98%）。
この +5,336 万円は「1 日で株価が 60% 上がった」ことを意味してしまうが、実際は
iSPEED の 43 銘柄を登録した際に、その銘柄が元々持っていた含み益（+4,215 万円）が
差分に混入したもの。取得原価の差だけでは、追加銘柄が持ち込む含み損益を除去できない。

対処として、期間中に銘柄数または取得原価が変わった場合は gainDelta を null にし、
画面に「この期間に +43 銘柄の変動があったため、株価変動分は分けて出せません」と明示する。
構成が変わっていない期間のみ正確な株価変動分を表示する。

判定には 0.01% の許容差を設けている（為替換算や小数株による誤差を構成変化と誤認しないため）。

### 比較対象の選び方
株価更新は 1 日に複数回走るため、直前の記録と比べると「数分前との差」になり無意味。
直近から 20 時間以上離れた最初の記録を比較対象にする（全て同日中なら最古の記録）。

## 38. IBKR（Interactive Brokers）シンガポールに対応する
背景: 3 つ目の証券プラットフォーム。米国株と日本株が同一口座に混在し、
基軸通貨 SGD で評価額が換算表示される特殊な構造を持つ。

- [x] broker enum に `ibkr` を追加し DB スキーマを更新する
- [x] BROKER_LABELS / BROKER_STYLES / BROKER_HEX に IBKR を追加する（色は青系）
- [x] IBKR のレイアウト定義を brokerFormats.ts に追加する
- [x] 銘柄コードから通貨を判別する（4 桁数字 = JPY、英字ティッカー = USD）
- [x] K 表記の数量（6.90K = 6,900）を正しく展開する
- [x] 評価額で検算して K 表記の丸め誤差を検証する
- [x] 12 銘柄を登録して元画面と照合する（のちに全 51 銘柄へ拡張）
- [x] テストを追加する

### 判明した通貨の構造（ORCL 詳細画面で確認）
- 現在値・平均価格 → **現地通貨**（ORCL は「150.77 USD x 100」と明記）
- 市場価格（評価額） → **SGD**（「185.1K SGD」と明記）
- 検算: 960 株 × 150.77 USD = 144,739 USD、185.1K SGD ÷ 144,739 USD = 1.279 SGD/USD（実勢と一致）

したがって SGD はシステム内では扱わず、USD と JPY で保持する。
~~SGD/JPY レートの追加は不要。~~

> **訂正（2026/8/16）**: この判断は誤りだった。追加のスクリーンショットで
> SGX 銘柄 7 件（SGD 建て）を保有していることが判明したため、SGD/JPY レートの
> 取得と SG 市場の追加を実装した。詳細はセクション 30 を参照。

## 39. 信用取引（レバレッジ）に対応する
背景: IBKR で円を借り入れて株を買っている（信用取引）。株式の時価をそのまま
資産計上すると総資産が約 2 億円過大になる。負債を記録して純資産で見る必要がある。

- [x] 口座別の負債・証拠金を保持するテーブルを追加する
- [x] 借入額を通貨別に記録できるようにする（JPY 建ての借入）
- [x] 純資産（株式時価 − 借入）を算出する
- [x] レバレッジ倍率（株式時価 ÷ 純資産）を算出する
- [x] 証拠金維持率と追証までの下落率を算出する
- [x] ダッシュボードに純資産・レバレッジ・証拠金余力を表示する
- [x] 現物のみの口座（楽天・moomoo）には表示しない
- [x] 設定画面から借入額・維持証拠金を更新できるようにする
- [x] テストを追加する（server/leverage.test.ts・server/leverage.ibkr.test.ts）

### IBKR の実測値（2026/8/16 時点、SGD 建て）
| 項目 | 金額 |
|---|---|
| 有価証券総ポジション価値 | 4,027,724.59 |
| 現金（借入） | −1,826,237.33 |
| 純資産評価額 | 2,204,556.91 |
| 維持証拠金 | 844,670.35 |
| 維持証拠金余力 | 1,359,886.56 |
| 月初来利息 | −1,217.22 |
| JPY 残高（借入の内訳） | −228,720,494.5 円 |

算出される指標: レバレッジ 1.83 倍、証拠金維持率 261%、追証まで約 34% の下落余地。

## 30. シンガポール市場（SGX）対応と IBKR 全 51 銘柄の登録
当初 12 銘柄と認識していたが、IBKR の一覧は時価評価額の降順で表示されるため画面下に
続きがあり、追加のスクリーンショットで **全 51 銘柄**であることが判明した。
うち 8 銘柄がシンガポール株（SGX）で、3 つ目の市場・通貨への対応が必要になった。
読み取り原本は docs/ibkr-holdings-source.md に保存済み。

- [x] Market 型に `SG` を追加し、SGX 銘柄を日本株・米国株と区別する
- [x] SGD/JPY レートを自動取得し、SGD 建て銘柄を円換算する
- [x] 取引所コード（TSEJ / NYSE / NASDAQ.NMS / SGX）から通貨を自動判定する
- [x] IBKR の 51 銘柄を登録し、既存の 12 銘柄を置き換える
- [x] IBKR の株式時価が画面の 4,027,724 SGD と一致することを検算する
- [x] レバレッジが画面表示と同じ約 1.83 倍になることを確認する
- [x] 通貨別現金残高（JPY −228,720,494 / SGD 6,585 / USD 2,495）を登録する
- [x] 現物のみの口座（楽天・moomoo）には信用情報を表示しないことを確認する

### 実装後の検算結果（2026/8/16、SGD/JPY = 124.564）

| 項目 | システム計算 | IBKR 画面 | 誤差 |
|---|---|---|---|
| 株式時価 | 4,029,311 SGD | 4,027,724 SGD | +0.04% |
| 借入 | 1,826,237 SGD | 1,826,237 SGD | 0.00% |
| 純資産 | 2,203,073 SGD | 2,204,557 SGD | −0.07% |
| レバレッジ | 1.829 倍 | 1.83 倍 | 一致 |

全体では 134 口座レコード（103 銘柄）、株式時価 7.67 億円・借入 2.27 億円・
純資産 5.41 億円・全体レバレッジ 1.42 倍。市場別は日本株 62 銘柄 4.36 億円 /
米国株 34 銘柄 2.90 億円 / シンガポール株 7 銘柄 0.41 億円。

### 読み取り原本の訂正 2 件

| コード | 当初の記録 | 正しい銘柄 | 判断根拠 |
|---|---|---|---|
| 373A | 東京メトロ | LIPPS | 東京メトロは 9023。373A.T の株価 × 数量が画面の評価額と一致 |
| M44U | Mapletree Pan Asia Commercial Trust | Mapletree Logistics Trust | Pan Asia Commercial は N2IU。株価 API の名称と評価額が一致 |

市場別の内訳も「日本株 25 / 米国株 18 / SGX 8」と記録していたが、
実際に数えると「日本株 21 / 米国株 23 / SGX 7」だった（合計 51 は一致）。

## 31. 設定画面から借入額・維持証拠金を更新する
借入額は利息と売買で日々動くため、残高画面のスクショを見ながら数字を直せる必要がある。

- [x] 口座を選んで借入額・維持証拠金・支払利息を入力できるようにする
- [x] 口座の基軸通貨（IBKR は SGD）で入力し、円換算はサーバー側で行う
- [x] 保存前に純資産とレバレッジを試算表示し、桁の入力ミスに気付けるようにする
- [x] 記録がある口座に「記録あり」バッジを出す
- [x] 信用取引をやめた場合に記録を削除できるようにする
- [x] 部分更新で通貨別内訳・検算値が消えないようにする

### 実装中に見つけて直した不具合
借入額だけを保存し直すと `currencyBreakdown`（借入の通貨別内訳と、
IBKR 画面表示の株式時価・純資産という検算用の値）が null で上書きされ、
後から検算できなくなっていた。保存時に既存の記録を読み込んでマージするよう修正し、
消えた値を復元した。挙動はテスト 5 件で固定した。

## 32. 渣打銀行 シンガポール（SC Mobile Trading）に対応する
4 つ目の証券口座。基軸通貨は SGD、現物のみで信用取引は使っていない（本人確認済み）。
TSE 3 銘柄 + SGX 7 銘柄 = 10 銘柄。

- [x] 口座種別に `sc_sg` を追加（ラベル・短縮名・配色・基軸通貨 SGD）
- [x] DB スキーマの broker enum を BROKERS 定数から生成するようにして追加漏れを防ぐ
- [x] broker enum に sc_sg を追加するマイグレーションを適用
- [x] OCR レイアウト定義を追加（SC Mobile Trading の縦積みカード形式）
- [x] 実画面 4 枚から読み取り、セクション小計との整合性を検証
- [x] 10 銘柄を登録し、口座合計 SGD 357,269.41 と突き合わせて検算（差 0.06%）
- [x] 既存口座と重複する銘柄が合算表示されることを確認（6 件すべて整合）
- [x] 現物のみの口座なのでレバレッジ項目を表示しないことを確認

### 画面構造の特徴（moomoo / iSPEED / IBKR との違い）
表形式ではなく銘柄ごとのカード形式で、1 銘柄が 2 行 × 2 列のラベル付きで並ぶ
（Mkt Price / Total Qty / Mkt Value / Unrealized P/L）。市場ごとに TSE・SGX の
セクション見出しがあり、セクション小計が表示される。銘柄コードは `7270.JP`
`D05.SG` のように市場サフィックス付きで、これを市場判定に使える。
タップで展開すると Avg Price（平均取得単価）も出る。

### 平均取得単価の扱い
カードを畳んだ状態では Avg Price が出ない。含み損益と評価額から
`取得原価 = 評価額 − 含み損益`、`平均単価 = 取得原価 ÷ 数量` で逆算できるため、
この方法で登録する。展開済みの SUBARU（JPY 2,965.92）で逆算値を検証する。

### 検証の結果、逆算方法を変更した
当初考えた「評価額 − 含み損益」方式は誤差 0.174% だったが、
「含み損益 ÷ 損益率」方式は 0.037% で 5 分の 1 に収まった。
現在値が遅延値かつ丸められているため、それを使わない後者を採用した。

ただし損益率も小数第 2 位までの表示なので、率が小さい銘柄（U11 の +3.58% など）
では取得原価に ±0.3% の不確かさが残る。結果として**評価額は画面と 0.06% 差で
一致したが、含み損益は +1.56%（+875 SGD）ずれた。**
これは画面の表示桁数の限界で、逆算方法を変えても解消しない。
資産額の把握には影響しないため現状のまま運用し、含み損益を正確にしたい場合は
各カードを展開した画面をもらって Avg Price を上書きする方針とした。

### 登録結果
全体で 107 銘柄・144 口座レコードになった（渣打 10 銘柄・4,453 万円）。
市場別は日本株 63 / 米国株 34 / シンガポール株 10 銘柄。
渣打の銘柄のうち 6 件が他口座と重複しており、合算値と内訳がすべて整合した。

## 33. 配当情報を扱えるようにする
シンガポール株に REIT が多く配当が重要。全 107 銘柄の配当を把握し、
年間の受取見込み額を出せるようにする。

- [x] 配当データの取得元を調査し、107 銘柄でどれだけ取得できるか検証する（Yahoo Finance の chart API に `events=div,split` を付けて実配当履歴を取得。全 113 銘柄で成功、配当あり 94 / 無配 13）
- [x] 日本株・米国株・シンガポール株それぞれで実際の配当と合っているか確認する（トヨタ 95 円・NTT 5.2 円・DBS 2.22 SGD などを外部情報と照合）
- [x] DB スキーマに配当情報を追加（annualDividend / dividendCount / lastDividendDate / lastDividendAmount / dividendUpdatedAt / hasSpecialDividend / recurringDividend）
- [x] 保有株数から年間受取配当額を計算するロジックを実装する（server/services/dividend.ts）
- [x] 外貨建て配当を円換算して合算できるようにする（既存の fx.ts を使い USD/SGD を円換算）
- [x] 株価更新と同時に配当情報も更新する（配当は日次で変わらないため 7 日以内は再取得しない。手動の「配当更新」で強制取得も可能）
- [x] ダッシュボードに年間配当額（見込み）と全体利回りを表示する（年間 ¥20,033,968 / 月あたり ¥1,669,497 / 現在値利回り 2.47% / 取得原価利回り 2.93%）
- [x] 口座別・市場別の配当内訳を表示する（合計はいずれも全体と誤差 0 円で一致）
- [x] 保有一覧に銘柄ごとの配当額と利回りを表示する（スマホはカード、PC は「年間配当」列。複数口座の内訳行にも口座別の配当額）
- [x] 配当利回りの高い銘柄・配当額の大きい銘柄が分かるようにする（「配当額順」「配当利回り順」の並び替えを追加）
- [x] 無配銘柄と配当情報が取得できなかった銘柄を区別して表示する（PC 表で「無配」「未取得」を書き分け、サマリーにも件数を表示）
- [x] テストを追加する（配当計算 15 件 + 配当集計 8 件）

## 34. 増えた銘柄のニュース取得と AI 分析
口座が 4 つ・107 銘柄に増えたため、シグナル未生成の銘柄が多く残っている。

- [x] 現在のシグナル生成状況（未生成の銘柄数）を確認する（実行前は 37/107 生成済み・70 銘柄が未生成）
- [x] 全銘柄のニュースを取得する（分割実行）。601 件を取得して AI で影響度分析。27 バッチ・29.4 分。ニュース総数 1,265 件、95 銘柄にニュースあり
- [x] 全銘柄の AI 分析を実行してシグナルを生成する（分割実行）。18 バッチ・22.5 分で 107 銘柄すべて成功、失敗 0 件
- [x] 生成結果を確認する（ADD 23 / HOLD 40 / WATCH 44、REDUCE・EXIT は 0 件）
- [x] 失敗した銘柄があれば原因を特定して再実行する（失敗 0 件。ニュース取得中に Google への接続タイムアウトが 2 件あったが、リトライで取得できた）

## 35. 配当の受取月を月別グラフで表示する
どの月に配当が集中するか分かるようにする。

- [x] 配当の支払月別データを保存する（holdings.monthlyDividends に 12 か月分の JSON）
- [x] 月別の受取見込み額を集計するロジックを実装する（buildMonthlyDividends）
- [x] ダッシュボードに月別配当グラフを追加する（棒グラフ + 月あたり平均の線）
- [x] 通貨が混ざるため円換算して合算する（USD / SGD を円換算して加算）
- [x] 最も配当が多い月と「上位 3 か月の集中度」を表示する
- [x] 市場別カードに「配当が多い月」と偏りの有無を表示する
- [x] テストを追加する（月別集計 6 件）

**検算結果**：月別の合計 ¥20,033,968 = 年間配当（誤差 0 円）。市場別の月別合計も全体と誤差 0 円。

**配当の月別分布**（上位 3 か月に 68.2% が集中）

| 月 | 金額 | 割合 |
|---|---|---|
| 3 月 | ¥6,500,447 | 32.4% |
| 9 月 | ¥5,872,050 | 29.3% |
| 11 月 | ¥1,299,598 | 6.5% |
| 5 月 | ¥1,179,295 | 5.9% |
| 4 月 | ¥1,143,839 | 5.7% |

市場別の傾向：日本株は 3 月に 49% が集中（3 月 595 万・9 月 562 万）。
米国株は四半期ごとに分散し最多月でも 12%。シンガポール株は 4 月・5 月・11 月が中心。
偏りの主因は日本株で、日本株だけで 3 月・9 月に 1,157 万円が入る。

## 36. 借入金利と配当の比較判定
IBKR は借入があるため、配当が金利負担を上回っているかを判定する。

実際の借入は **日本円 228,720,494 円**（IBKR 画面は基軸通貨 SGD 換算で -1,826,237 SGD と表示）。
金利は JPY のレートが適用される。IBKR Pro の JPY マージンローンは階層別：

| 借入額（JPY） | 年率 |
|---|---|
| 0 〜 1,100 万 | 2.330%（BM + 1.5%） |
| 1,100 万 〜 1 億 1,400 万 | 1.830%（BM + 1.0%） |
| 1 億 1,400 万 〜 57 億 | 1.580%（BM + 0.75%） |
| 57 億 〜 230 億 | 1.330%（BM + 0.5%） |

検算: 2.287 億円なら加重平均 約 1.60%、年間 約 366 万円。
画面の「月初来利息 -1,217 SGD」（約 15.2 万円 / 16 日分）から年換算すると約 347 万円で整合する。

- [x] 借入通貨と借入額を通貨単位で記録できるようにする（currencyBreakdown から自動判定）
- [x] IBKR の階層別金利テーブルを実装し、加重平均の年率を計算する（server/services/marginInterest.ts）
- [x] 年間の金利負担額を計算する（借入通貨で計算し円換算）
- [x] IBKR の年間配当と金利負担を比較して判定する（POSITIVE / THIN / NEGATIVE）
- [x] ダッシュボードの IBKR カードに比較結果を表示する
- [x] サマリーの年間配当カードにも「配当 − 利息」の手取りを表示する
- [x] 実際の請求額（月初来利息）と比べて計算が合っているか検証する
- [x] テストを追加する（金利計算 15 件）

**計算結果と検算**

| 項目 | 値 |
|---|---|
| 借入（円換算） | ¥227,483,427（JPY 建て） |
| 加重平均の年率 | 1.7295% |
| 年間の利息 | ¥3,934,238 |
| 月初来の実績利息 | ¥151,622 → 年換算 ¥3,458,872（計算値との差 −12.1%） |
| IBKR の年間配当 | ¥12,389,972 |
| 配当 − 利息 | **+¥8,455,734**（配当は利息の 3.15 倍・POSITIVE） |
| 全体の手取り | ¥20,033,968 − ¥3,934,238 = **¥16,099,730**（手取り利回り 1.98%） |

階層別の内訳：1,100 万 × 2.33% = ¥256,300 / 1 億 300 万 × 1.83% = ¥1,884,900 /
1 億 1,348 万 × 1.58% = ¥1,793,038。

**判明した設計上の注意点**
- IBKR の金利は**累進方式**（階層別の加重平均）。最下層の 1.580% で全額計算すると
  年間 34 万円ほど過小評価になる。当初 todo に書いた「約 1.60% / 366 万円」も
  この誤りを含んでいたため、正しくは 1.7295% / 393 万円。
- 金利は**借入通貨**のレートで決まる。IBKR は基軸通貨 SGD で借入額を表示するが、
  実際の建玉が JPY なら JPY のレートを使う。currencyBreakdown で最も大きな
  マイナス残高の通貨を借入通貨として自動判定する実装にした。
- 月初来実績からの年換算が計算値より 12% 低いのは、月の途中で借入額が変動した場合や
  利息計上のタイミング差によるもの。目安としての併記に留める。
- CarryVerdict の型・ラベル・配色は shared/investing.ts に置いた。
  MarginRisk で「サーバー側とクライアント側に別々に定義して片方だけ増える」問題が
  起きたため、同じ形に揃えている。

### 配当機能の実装の判断（検証して決めた）
- **実績を使う**。`trailingAnnualDividendRate`（実績）と `dividendRate`（予想）は
  quoteSummary API でしか取れず、Data API 経由では 404 になる。代わりに chart API の
  配当イベントから直近 1 年の実支払額を合計した。予想より保守的で、REIT のように
  変動が大きい銘柄でも実態に近い。
- **税引前で表示する**。税率は口座の所在地・租税条約・NISA の有無で変わり、
  同じ銘柄でも口座ごとに違う。誤った税引後額を出すより、税引前と明記して出す方が誠実。
- **株式分割の補正は入れない**。Yahoo は配当額も株価も分割調整済みで返すため、
  補正すると逆に過小評価になる（トヨタの 10:1 分割で検証）。当初入れた補正は削除した。
- **特別配当（記念配当）を検出して注記する**。年 3 回以上支払がある銘柄は
  中央値の 2.5 倍を超える回を特別配当とみなす。年 2 回の銘柄は検出できないため、
  利回り 8% 超を安全網として警告する（日本製鉄 10.65% がこのケース）。

## 37. 月別配当グラフのタップで銘柄内訳を見られるようにする
ユーザー要望: 「タップしたらどの月にどの株が配当があるかわかるといいね」

- [x] 月別の配当を銘柄単位で返す API を追加する（月・銘柄名・口座・金額・市場）
- [x] グラフの棒をタップするとその月の銘柄一覧が開くようにする
- [x] 一覧は金額の大きい順に並べ、口座と市場が分かるようにする
- [x] スマホで棒が細くて押しにくい問題に対応する（月を選ぶ別の手段も用意）
- [x] 同一銘柄を複数口座で持つ場合は口座ごとに分けて表示する
- [x] テストを追加する

実装内容:
- `server/services/dividendCalendar.ts` に `buildDividendCalendar()` を追加。
  12 か月分を必ず返し、配当が無い月も要素として残す（「その月は配当が無い」ことも情報）。
- 現地通貨の額は円換算額から逆算せず、1 株配当 × 株数で計算する。
  逆算すると為替レートを持ち回る必要があり丸め誤差も乗るため。
- グラフの棒のクリックと、上部の月ボタン列の両方で月を選べる。
  スマホでは棒が細く押しにくいため導線を二重に用意した。
- 選択中の月の棒だけを濃く（他は不透明度 0.3）して対応関係を示す。
- 件数が多い月（3 月は 85 件）は既定で上位 10 件のみ表示し、
  「上位 10 件でこの月の 40% を占めます」と累計割合を添える。
- 検算: カレンダー合計 ¥20,033,968 = 月別合計と誤差 0 円。
  月ごとの不一致 0 件、各月の entries 合計と月合計の不一致 0 件。
- テスト 11 件追加（合計 320 件パス）。

---

## 38. 保有一覧の見やすさ改善（内訳行の色分け・前日比の削除）
ユーザー要望:
- 「これ一覧２口座 ある場合は 色別で 後ろに背景の色入れた方が見やすい? ぱっと見 見ずらいと思って」
- 「前日比いらないよね?」（長期保有のため）

- [x] 複数口座の内訳行に薄い背景色を敷き、合計行と区別する
- [x] 銘柄のまとまりごとに区切りを明確にする（隣の銘柄との境目が分かるように）
- [x] 内訳行に口座の色を出す（口座ごとに色が決まっているので左端に細い縦線など）
- [x] 保有一覧の表から「前日比」列を削除する
- [x] 前日比を削除した分の横幅を評価損益・年間配当に回す
- [x] スマホのカード表示でも同じ考え方で内訳を見やすくする
- [x] 前日比の削除で他の画面が壊れていないか確認する（ダッシュボード・投資カード等）
- [x] テストを追加・更新する

実装内容:
- 内訳行の背景を `bg-muted/25` → `bg-muted/40` に濃くし、合計行（白）との差を明確にした。
- 内訳行の左端に口座のブランドカラーの帯（幅 4px）を出した。
  BROKER_HEX を使うため色の定義は 1 箇所のまま（渣打=teal / IBKR=violet / moomoo=orange / 楽天=red）。
- 複数口座の銘柄は合計行の上に `border-t-2` を置き、隣の銘柄との境目を明確にした。
- スマホの内訳（BrokerBreakdown）にも同じ色の帯を付け、口座ごとの年間配当も表示した。
- 「前日比」列と「前日比順」の並び替えを削除（長期保有では判断材料にならない）。
  列数が 12 → 11 になったため空表示の colSpan も修正した。
- 削除した列の幅を評価損益（min-w-130）と年間配当（min-w-120）に回し、
  「+SGD 8,758.20」のような桁の多い数字が折り返さないようにした。
- テスト 320 件パス（既存の内訳行テストも維持）。

---

## 39. 配当専用ページの新設
ユーザー要望: 「配当一覧も別でページあった方がいい?」→ ダッシュボードが縦に長くなりすぎているため分離する

方針:
- ダッシュボードには「年間いくら入るか」「金利を賄えているか」の結論だけを残す
- 配当ページに月別一覧・銘柄別ランキング・利回り順・絞り込み・減配リスクを集約する

- [x] `/dividends` ページを新規作成しサイドバーに追加する
- [x] 月別の受取額を一覧で表示する（12 か月分、銘柄内訳を展開可能）
- [x] 銘柄別の年間配当ランキングを表示する（金額順・利回り順の切り替え）
- [x] 口座別・市場別で絞り込めるようにする（「IBKR の日本株だけ」等）
- [x] 特別配当が含まれる銘柄を明示し、除いた場合の金額も出す
- [x] ダッシュボードの配当セクションを簡素化し、配当ページへの導線を置く
- [x] スマホでの見やすさを確認する（横スクロールなし）
- [x] テストを追加する

実装内容:
- `/dividends` を新設し、サイドバーに「配当」を追加した。
- 「月から見る」「銘柄から見る」の 2 モードを用意した。
  月モードは 12 か月のボタン列＋棒グラフで、選んだ月の銘柄内訳を金額順に出す。
  銘柄モードは同一銘柄を複数口座で持つ場合に合算し、口座バッジを並べる。
- 絞り込みは口座・市場・銘柄名の 3 つ。URL クエリ（?broker=ibkr&market=JP）にも対応。
  絞り込むと合計・利回り・銘柄数を**絞り込み後の値で再計算**する（全体の何 % かも併記）。
  全体の値をそのまま出すと、絞ったのに全体額が見えて誤解を生むため。
- 銘柄モードには「入る月」と年間の回数を出した（年 1 回 / 2 回 / 4 回が一目で分かる）。
- 並び替えは配当額順・利回り順・買値に対する利回り順・銘柄名順。
- 特別配当を含む銘柄には警告アイコンを付け、除いた場合の年間額も表示する。
- ダッシュボードの年間配当カードに「配当ページで詳しく見る」の導線を追加した。

検算:
- カレンダー合計 ¥20,033,968・銘柄ビュー合計 ¥20,033,968 がいずれも
  全体の年間配当と誤差 0 円で一致。配当あり銘柄数 94 も一致。
- 絞り込みの実測値: IBKR 日本株 ¥6,242,780 / 20 銘柄、IBKR シンガポール ¥2,021,986 / 7 銘柄、
  渣打 シンガポール ¥1,328,845 / 7 銘柄、楽天 米国株 ¥312,553 / 15 銘柄、
  moomoo 日本株 ¥2,290,850 / 25 銘柄。
- テスト 8 件追加（絞り込みの独立性・合計の再計算・検索・並び順）。全 328 件パス。
