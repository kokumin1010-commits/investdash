# Watchlist 长期年线视觉验收记录

## 首轮正式验收（2026-09-12）

正式版本 `4dcc955` 已在 390px 与 1280px 显示以下正确状态：长期年线位于公司名称／代码下方，默认展开，默认选择「上場来」，目标价格摘要与现有观察卡资料保持一致，页面无横向溢出。

首轮截图一度只显示加载占位区域。复核正式 `portfolio.longTermChart` 后确认 ITW「上場来」数据可在约 2.3 秒返回；根因是验收脚本只等待展开容器出现，没有等待数据摘要和 SVG 路径完成渲染，并非页面组件故障。脚本现已改为同时等待「表示期間」及 SVG 路径，再进行截图与判定。

最终复验确认：Watchlist 的 ITW 年线位于名称／代码下方，默认展开并选中「上場来」，显示 1984–2026 年完整路径、当前价、高点回撤及目标价格线；Buy Plans 的 TXN 同样位于名称／代码下方并显示 1984–2026 年完整路径。390px 与 1280px 均无横向溢出。

| 视口 | 名称下方 | 默认上場来 | 无横向溢出 | 图线完整 |
|---:|---|---|---|---|
| 390px | 通过 | 通过 | 通过 | 通过 |
| 1280px | 通过 | 通过 | 通过 | 通过 |
